<h1>Artist Index</h1>
<ul>
    <% @artists.each do |artist| %> 
      <h2><li><%= link_to artist.name, artist %> - <%= artist.song_count %> 
      <%= "song".pluralize(artist.song_count) %></li></h2>
    <% end %>
</ul>


