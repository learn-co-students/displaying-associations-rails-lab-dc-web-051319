<h1>Show</h1>


    <%= link_to @artist.name, artist_path(@artist) %>
    <div><%= @artist.song_count %> songs</div>
    
    <ul>
    <% @artist.songs.each do |song| %>
        <li>
        <%= link_to song.title, song_path(song) %>
        </li>
    <% end %>
    </ul>